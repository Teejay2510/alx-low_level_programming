#include "main.h"
/**
* print_number - prints an integer
* @n: integer to be printed
*/
int main(void)
{
	int i, res;

	for (i = 0; i < 1024; i++)
		if (i % 3 == 0 || i % 5 == 0)
			res += i;
	printf("%d\n", res);
	return (0);
}
